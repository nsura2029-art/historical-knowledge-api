# apps/api/src/routes/v1/AGENTS.md — the v1 endpoint index

> **Source of truth for what's in the API.** Every endpoint we ship gets an entry here. New endpoint? Add to this file. Deprecated endpoint? Mark `**DEPRECATED**:` here and keep the entry until the route is removed.

## Live endpoints (28+, as of 2026-08-05)

### Browse & detail

| Method | Path | What it returns |
|---|---|---|
| GET | `/v1/people` | Browse/filter people. Query: `?profession=&country=&generation=&page=&limit=` |
| GET | `/v1/people/{slug}` | Person detail. Slug redirects supported (e.g. `donald-j-trump` → `donald-trump`) |
| GET | `/v1/people/{slug}/timeline` | Chronological life events. Each event has `source` (tier + name + URL) |
| GET | `/v1/people/{slug}/related` | Multi-signal relevance: same profession +0.40, country +0.20, generation +0.10, star_sign +0.05, chinese_zodiac +0.05, era overlap +0.15, shared relation +0.25, popularity +0.05. Max 0.95. |
| GET | `/v1/people/{slug}/references` | Sources grouped by source_registry, sorted by tier A first. Each source has `landing_page` + per-article URL + claims supported. |

### Entity pages (9)

| Method | Path | What it returns |
|---|---|---|
| GET | `/v1/places/{slug}` | Place page. Uses `country_code` directly (no separate country table). |
| GET | `/v1/countries/{code}` | Country page. People born/died in this country. |
| GET | `/v1/professions/{slug}` | Profession page. People with this profession. |
| GET | `/v1/generations/{slug}` | Generation page (Pew + McCrindle taxonomy). |
| GET | `/v1/star-signs/{slug}` | Star sign page. |
| GET | `/v1/chinese-zodiacs/{animal}` | Chinese zodiac page. |
| GET | `/v1/causes-of-death/{slug}` | Cause of death page. |
| GET | `/v1/works/{slug}` | Notable work page. |
| GET | `/v1/awards/{slug}` | Award page. |

### Facets (10)

| Method | Path | Notes |
|---|---|---|
| GET | `/v1/facets/professions` | Profession counts |
| GET | `/v1/facets/countries` | Country counts |
| GET | `/v1/facets/generations` | Generation counts |
| GET | `/v1/facets/star-signs` | Star sign counts |
| GET | `/v1/facets/chinese-zodiacs` | Zodiac counts |
| GET | `/v1/facets/causes-of-death` | Cause of death counts (v2) |
| GET | `/v1/facets/eras` | Era counts (v2) |
| GET | `/v1/facets/awards` | Award counts (v2) |
| GET | `/v1/facets/industries` | Industry counts (v2) |
| GET | `/v1/facets/fame-categories` | Fame category counts (v2) |

### Search & admin

| Method | Path | Notes |
|---|---|---|
| GET | `/v1/search?q=...` | 7-tier match scoring: canonical 1.0, alias 0.85, description 0.6, work/award 0.7, external_id 0.5, profession 0.4. |
| GET | `/v1/admin/people/{id}/{publish,unpublish,recompute-derived,compute-quality,quality-gates}` | 11 admin ops. Open auth (TODO: lock before production). |

### Years, birthdays, system

| Method | Path | Notes |
|---|---|---|
| GET | `/v1/years/{year}` | People born/died + events in this year. Uses `substr(start_date, 1, 4) = ?`. |
| GET | `/v1/birthdays/{mm}/{dd}` | People born on this mm-dd. |
| GET | `/v1/on-this-day/today` | Today view (year-agnostic, top 5 per section). 14 sections: events, births, deaths, weddings, divorces, holidays + 8 category sections (sports, science, tech, politics, cultural, religion, economic, royal, disasters, social, exploration, crime). |
| GET | `/v1/on-this-day/{mm_dd}` | Same shape, for any mm-dd. Optional `?from&to&country&category&limit` query. |
| GET | `/v1/on-this-day/date/{yyyy_mm_dd}` | Year-list view (specific date). 1700-2100 (Tier 1). Returns 400 for out-of-range. |
| GET | `/v1/health` | Liveness check. |
| GET | `/v1/version` | Worker version + commit. |
| GET | `/v1/openapi.json` | OpenAPI 3.1 spec, generated by `@hono/zod-openapi`. |
| GET | `/v1/docs` | Swagger UI. |

## Not yet built (MVP blockers)

- **GET `/v1/on-this-day/events/{id}`** — single event detail. (TASK-013 follow-up)
- **GET `/v1/events/{id-or-slug}`** — generic event detail. (TASK-012)
- **GET `/v1/feeds/on-this-day.rss`** — RSS feed. (Phase 5)
- **GET `/sitemap.xml`** — segmented sitemap. (Phase 5)
- **Rate limit middleware** — TASK-018.

## Roadmap: new endpoints coming in Q1 (KP-003, KP-004, KP-007)

The 24-task KP roadmap (see root `AGENTS.md` and `docs/architecture/00-DOX-ALIGNMENT-NOTES.md`)
adds these endpoints. They land in Q1 of Year 1.

### KP-003 (atomic-claim model)

```
GET  /v1/claims/{id}                              # single claim detail
GET  /v1/entities/{id-or-slug}/claims              # all claims for an entity
GET  /v1/entities/{id-or-slug}/conflicts           # claim conflicts for an entity
GET  /v1/claim-conflicts                           # all open conflict groups
```

### KP-004 (evidence graph + citation export)

```
GET  /v1/sources/{id}                              # full source detail (existing, enriched)
GET  /v1/entities/{id-or-slug}/sources             # existing endpoint, claim-level drilldown
GET  /v1/claims/{id}/sources                       # all sources for a specific claim
GET  /v1/sources/{id}/claims                       # all claims citing a source
GET  /v1/entities/{id-or-slug}/citations?format=  # BibTeX/RIS/CSL JSON export
POST /v1/sources/{id}/refresh                      # re-fetch (admin)
POST /v1/admin/editorial-revisions                 # record a change
GET  /v1/entities/{id-or-slug}/revisions           # change history
```

### KP-007 (media + rights pipeline)

```
GET    /v1/entities/{id-or-slug}/media             # all approved media for an entity
GET    /v1/media/{id}                              # media detail with rights
GET    /v1/media/{id}/download                     # signed R2 URL (or 403)
GET    /v1/r2/{r2_key}                             # direct R2 access
POST   /v1/admin/media/upload                      # upload new (admin)
POST   /v1/admin/media/{id}/approve                # approve rights (admin)
POST   /v1/admin/media/{id}/reject                 # reject rights (admin)
```

### Display gate (enforcement, KP-007)

Every API response that includes `media_assets` MUST filter by
`media_rights.status = 'approved'`. No image = OK. Rights-unknown image =
blocked at query level. See `packages/db/AGENTS.md` for the query pattern.

## On This Day API gotchas (TASK-013)

- **Hono path params can't have hyphens** — use `{mm_dd}` and `{yyyy_mm_dd}` in the route definition, the URL still has hyphens. The schema uses underscore.
- **Year-list view uses `/v1/on-this-day/date/{yyyy_mm_dd}`** — not `/v1/on-this-day/{yyyy-mm-dd}` to avoid the mm-dd regex matching first.
- **Tier 1 boundary**: yyyy-mm-dd returns 400 for years < 1700 or > 2100. mm-dd doesn't check year.
- **mm-dd "13-99" passes the regex but is an invalid date** — return 200 with empty arrays, not 400. The page should always render.
- **`holiday_occurrence.calendrify_country_id`** maps to `country_cca2_map.calendrify_country_id` which gives us the cca2 code. The Calendrify source D1 is `timeandtimepro-full-v2`, read-only per HKA isolation.
- **`src_calendrify`** is the source_registry entry. Tier B (authority data set, CC-BY 4.0).
- **12 categories enforced via CHECK constraint** on `otd_event.category`. Adding a 13th category requires a migration.
- **`popularity_rank` is NOT a column on otd_event** — that was a copy-paste error. Don't add ORDER BY e.popularity_rank without a real column.

## Source IDs to remember (107 total)

Tier A (primary/official): `src_wikidata`, `src_frida_kahlo_foundation`, `src_bbc-news`, `src_nyt`, `src_the_white_house`, `src_usps`, `src_science_media_center`, `src_whitehouse_gov`, ...

Tier B (authority/open-knowledge): `src_viaf`, `src_library_of_congress`, `src_isni`, `src_orcid`, `src_geonames`, `src_mledoze`, `src_iana`, `src_un_m49`, ...

Tier C (domain): `src_crossref`, `src_openalex`, `src_musicbrainz`, `src_nobel`, `src_tmdb`, `src_imdb`, ...

Tier D (edited secondary, 35+ news outlets): `src_fox_news`, `src_npr`, `src_nyt`, `src_ap`, `src_bbc-news`, `src_newsweek`, `src_nbc_news`, `src_cnn`, `src_the-guardian`, `src_the_times_uk`, `src_pbs`, `src_reuters`, `src_washington_post`, `src_al_jazeera_english`, `src_dw`, `src_france_24`, `src_kyodo_news`, `src_xinhua`, `src_india_today`, ... (35+ in total)

Tier E (discovery only): `src_wikipedia` (and a few fan sites).

**Gotcha**: `src_bbc-news` (HYPHEN) not `src_bbc_news` (underscore). Same for `src_the-guardian` (HYPHEN).

**Gotcha**: `src_npr` = NPR. The user once typed "NOR" by mistake — it's NPR.

**Gotcha**: Wikidata IDs are in `external_identifier` table (e.g. `Q22686` for Donald Trump), not in the `entity` table. The `entity.id` is always `ent_<slug>`.

## Sort order conventions

- **References**: tier ascending (A first), then by article count descending.
- **Related**: relevance score descending (0.95 max).
- **Timeline**: `start_date` ascending (oldest first).
- **Search**: match score descending.
- **People browse**: `popularity_score` descending (0-100 scale, normalize to 0-1 in calculations).

## Per-route gotchas

- **`/v1/people/{slug}`** — see redirect handling code. 404 → check `slug_redirect` table → follow.
- **`/v1/people/{slug}/related`** — `popularity_score` is 0-100, must normalize to 0-1 in relevance scoring.
- **`/v1/people/{slug}/timeline`** — LEFT JOIN on `source_registry` for tier/name. Use `COALESCE(start_date, '9999-99-99')` in ORDER BY for null-safe sort.
- **`/v1/people/{slug}/references`** — groups by `source_registry.id`. Each source in the response has a `landing_page` (the base URL) AND per-article `external_url`.
- **`/v1/years/{year}`** — uses `substr(start_date, 1, 4) = ?` to filter on year prefix. No date math.
- **`/v1/search`** — 7-tier scoring is case-insensitive, uses `LOWER()` on both sides.
- **`/v1/admin/*`** — auth is currently OPEN. TODO: lock with CF Access or API key before public deploy.

## How to add a new endpoint

1. Add the route file in `apps/api/src/routes/` (or `routes/v1/` if v1-specific).
2. Update the per-endpoint index above.
3. Add a smoke test in the next `v#-smoke.mjs` script.
4. Update `apps/api/AGENTS.md` if a new directory is needed.
